{% extends "FriggKeeprBundle::layout.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block mode %}edit{%  endblock %}

{% block body -%}
    {{ form_start(form) }}
    <div class="row-fluid">
        <div class="span12">
            <div class="inline-block">
                {{ form_widget(form.topic, {'attr': {'class': 'input-xlarge', 'placeholder': 'Type a descriptive title'|trans}}) }}
            </div>
            <div class="inline-block">
                {{ form_widget(form.Language, {'attr': {'class': 'chosen margin-top-minus', 'data-placeholder': 'Syntax highlighting'|trans}}) }}
            </div>
            <div class="inline-block">

            </div>
        </div>
    </div>
    <div class="row-fluid">
        <div class="span12">
            {{ form_widget(form.content, {'attr': {'class': 'span12 code', 'placeholder': 'And paste your code here'|trans}}) }}
        </div>
    </div>
    <div class="row-fluid">
        <div class="span12">
            <ul class="tags pull-left" data-prototype="{{ form_widget(form.Tags.vars.prototype.name)|e }}">
                {% for tag in form.Tags %}
                    <li>{{ form_row(tag.name, {'attr': {'readonly': edit_tag}}) }}</li>
                {% endfor %}
            </ul>
            <ul class="inline pull-right">
            <li><label class="checkbox">{{ form_widget(form.private) }} {{ 'Private'|trans }}</label></li>
            <li>{{ form_widget(form.submit, {'attr': {'class': 'btn'}}) }}</li>
        </ul>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href='http://fonts.googleapis.com/css?family=Source+Code+Pro' rel='stylesheet' type='text/css'>
{% endblock %}
